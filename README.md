Texto Chat GPT

Necesito implementar en Java una versión optimizada del algoritmo de burbuja (Bubble Sort). La optimización consiste en marcar el último índice donde se realizó un intercambio, para que en la siguiente pasada no sea necesario comparar más allá de ese punto. Por favor, genera el código en Java con un método main que muestre cómo funciona ordenando un arreglo de ejemplo.
